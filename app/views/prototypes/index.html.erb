<main class="main">
  <div class="inner">
    <% if user_signed_in? %>
      <div class="greeting">
        こんにちははは、<%= link_to "#{current_user.name}さん", user_path(current_user.id), class: :greeting__link %>
      </div>
    <% end %>
      <div class="card__wrapper">
        <%= render partial: "prototype",locals: { prototype: @prototype } , collection: @prototype%>
      </div>
  </div>
</main>
